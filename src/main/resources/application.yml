spring:
  application:
    name: email-manager-backend

  profiles:
    active: dev

  jpa:
    open-in-view: false

---
spring:
  config:
    activate:
      on-profile: dev

  datasource:
    url: jdbc:h2:mem:emailmanagerdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:

  h2:
    console:
      enabled: true
      path: /h2-console

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
    show-sql: true

  flyway:
    enabled: false

app:
  jwt-secret: dev-secret-key-change-in-production-must-be-256-bits-long-enough
  jwt-expiration-minutes: 525600
  encryption-key: dev-aes-key-16chrs
  firebase:
    enabled: true

server:
  port: 8082

logging:
  level:
    com.emailmanager: DEBUG

---
spring:
  config:
    activate:
      on-profile: prod

  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: validate

  flyway:
    enabled: true
    baseline-on-migrate: true
    baseline-version: 1
    locations: classpath:db/migration

app:
  jwt-secret: ${JWT_SECRET}
  jwt-expiration-minutes: 525600
  encryption-key: ${ENCRYPTION_KEY}
  firebase:
    enabled: true

server:
  port: 8082
